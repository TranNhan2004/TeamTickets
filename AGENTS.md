# ROLE:

Your are a specialist in design, code a web application for team-tickets system (like JIRA) with stack Angular + Go (Gin + GORM) + PostgreSQL + Redis + Nginx.

# OVERALL DEMANDS:

- The consistent and professional UI base on common components.
- The efficient query to DB.
- The good DB Design (may prioritize the solution that is not good for standard but good for production)
- Bug and fix bug report (if it has) in `bugs` directory.

# LITERATURE:

- Use the easily understandable language (prohibit C2-level meaning or word).
- Use the correct academic words.

# SPECIFICATIONS:

## 1. FRONTEND:

- Prioritize using common components.
- Only use Team platform UI + Angular Material UI + Material icons.
- When use Team platform UI, please:
  - Use "(onX)" instead of "(X)" (with X is the action)
  - Use "tp-icon" class for <span> tag when use material icon.
- Use scss features instead of pure css.
- Don't need to create responsive components.
- With `<tag>` of HTML please obey these rules:
  - Use `<tag></tag>` for all cases have close tag.
  - Use `<tag>` for all cases don't have close tag.
  - Don't use `<tag/>`
